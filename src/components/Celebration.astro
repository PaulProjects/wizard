<div class="scoreboard">
  <div class="scoreboard__podiums" id="scoreboard_podium">
  </div>
  <ul class="scoreboard__items" id="scoreboard_items">
  </ul>
</div>

<style is:global>
  .number-holder {
    transition: all 500ms ease-in-out;
  }

  .scoreboard {
    width: 100%;
    border-radius: 5px;
    display: flex;
    max-width: 100vw;
    margin: 0 auto;
    padding: 2rem 20px;
    flex-direction: column;
    align-items: center;
  }
  .scoreboard__item {
    display: flex;
    padding: 1rem 0;
    width: 100%;
    margin-top: 2rem;
    flex-wrap: wrap;
    opacity: 0;
    top: 20px;
    transition: 500ms ease-in-out;
  }
  .scoreboard__item.is-visible {
    opacity: 1;
    top: 0;
    transition: 500ms ease-in-out;
  }
  .scoreboard__title {
    width: 100%;
    max-width: 100%;
    font-weight: 600;
  }
  .scoreboard__title small {
    color: #333;
  }
  .scoreboard__status {
    font-weight: 300;
  }
  .scoreboard__numbers {
    font-weight: 600;
    text-align: right;
    align-self: flex-end;
    margin-left: 20px;
    flex: 1;
  }
  .scoreboard__bar {
    width: 100%;
    margin-top: 1rem;
    background: #ececec;
    height: 10px;
    position: relative;
    border-radius: 5px;
    transition: all 500ms ease-in-out;
    display: flex;
    flex-basis: 100%;
  }
  .scoreboard__bar:before {
    content: "";
    width: 100%;
    flex-basis: 100%;
    clear: both;
    display: block;
  }
  .scoreboard__bar-bar {
    position: absolute;
    height: 10px;
    left: 0;
    top: 0;
    width: 0;
    border-radius: 5px;
    background: linear-gradient(to right, #f0cf65 0%, #ebc137 100%);
    transition: all 250ms ease-in-out;
  }

  .scoreboard__podiums {
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: flex-end;
    margin: 20px auto 100px auto;
    min-height: 350px;
    max-width: 600px;
    width: 100%;
    gap: 10px;
  }
  .scoreboard__podiums .scoreboard__podium {
    display: flex;
    flex-direction: column-reverse; /* Reverse order: name at bottom, base at top */
    align-items: center;
    flex: 1;
    max-width: 180px;
    min-width: 120px;
    opacity: 0;
    transform: translateY(-10px);
    position: relative;
    justify-content: flex-end; /* Align podium bases to the same baseline */
  }
  .scoreboard__podiums .scoreboard__podium.is-visible {
    transform: translateY(0);
    opacity: 1;
    transition: all 500ms ease-in-out;
  }
  .scoreboard__podiums .scoreboard__podium:nth-child(2) {
    margin-top: -50px; /* Make first place taller visually */
    z-index: 2; /* Ensure it's on top */
  }
  .scoreboard__podiums .scoreboard__podium-rank {
    font-size: 4rem;
    font-weight: 700;
    position: absolute;
    left: 50%;
    transform: translate(-50%, -50%);
    bottom: 0px;
  }
  .scoreboard__podiums .scoreboard__podium-base {
    height: 0;
    background: #ececec;
    color: #fff;
    width: 100%;
    min-width: 100px;
    max-width: 150px;
    border-radius: 5px;
    transition: all 1000ms ease-in-out;
    opacity: 0;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    order: 1; /* Ensure this comes first (top in reversed column) */
  }
  .scoreboard__podiums .scoreboard__podium-base.is-expanding {
    opacity: 1;
  }
  .scoreboard__podiums .scoreboard__podium-base--first {
    background: linear-gradient(to bottom, #bd4f6c 0%, #9e3b55 100%);
  }
  .scoreboard__podiums .scoreboard__podium-base--second {
    background-color: #f0cf65;
    background: linear-gradient(to bottom, #f0cf65 0%, #ebc137 100%);
  }
  .scoreboard__podiums .scoreboard__podium-base--third {
    background-color: #5e9ead;
    background: linear-gradient(to bottom, #5e9ead 0%, #49828f 100%);
  }
  .scoreboard__text {
    font-size: 1.2rem;
    font-weight: 600;
    margin: 0;
    word-wrap: break-word;
    overflow-wrap: break-word;
    hyphens: auto;
    line-height: 1.3;
    text-align: center;
  }
  .scoreboard__podiums .scoreboard__podium-number {
    text-align: center;
    font-size: 1.2rem;
    margin-bottom: 15px; /* Changed from margin-top to margin-bottom */
    width: 100%;
    padding: 0 5px;
    order: 2; /* Ensure this comes after the base */
  }
  .scoreboard__podiums .scoreboard__podium-number small {
    display: block;
    font-size: 60%;
    color: gray;
    line-height: 1.5;
    text-transform: uppercase;
  }
  @media (max-width: 768px) {
    .scoreboard__podiums {
      max-width: 100%;
      margin: 20px auto 80px auto; /* Increased bottom margin for name space */
      min-height: 280px;
      gap: 8px;
      padding: 0 15px;
    }
    .scoreboard__podiums .scoreboard__podium {
      min-width: 80px;
      max-width: 120px;
    }
    .scoreboard__podiums .scoreboard__podium:nth-child(2) {
      margin-top: -30px; /* Reduce first place elevation on mobile */
    }
    .scoreboard__podiums .scoreboard__podium-base {
      min-width: 80px;
      max-width: 100px;
    }
    .scoreboard__podiums .scoreboard__podium-number {
      font-size: 1rem;
      margin-bottom: 10px; /* Changed from margin-top to margin-bottom */
      min-height: 3em; /* Reserve space for wrapped names */
    }
    .scoreboard__podiums .scoreboard__podium-rank {
      font-size: 3rem;
    }
    .scoreboard__text {
      font-size: 1rem;
      line-height: 1.2;
    }
  }
  @media (max-width: 480px) {
    .scoreboard__podiums {
      min-height: 220px;
      gap: 5px;
      padding: 0 10px;
      margin: 20px auto 100px auto; /* Increased bottom margin for small screens */
    }
    .scoreboard__podiums .scoreboard__podium {
      min-width: 70px;
      max-width: 90px;
    }
    .scoreboard__podiums .scoreboard__podium:nth-child(2) {
      margin-top: -20px;
    }
    .scoreboard__podiums .scoreboard__podium-base {
      min-width: 70px;
      max-width: 80px;
    }
    .scoreboard__podiums .scoreboard__podium-number {
      font-size: 0.9rem;
      margin-bottom: 8px; /* Changed from margin-top to margin-bottom */
      min-height: 3.5em; /* More space for wrapped names on small screens */
    }
    .scoreboard__podiums .scoreboard__podium-rank {
      font-size: 2.5rem;
    }
    .scoreboard__text {
      font-size: 0.85rem;
      line-height: 1.1;
    }
  }

  .scoreboard__items {
    position: relative;
    margin: 0;
    padding: 0;
    list-style-type: decimal;
    width: 100%;
    max-width: 800px;
  }
</style>